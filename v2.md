# InsightFlow AI (v2) — Future Ideas

Potential future upgrades for InsightFlow AI:

- Add user authentication with Google OAuth or JWT.
- Add per-user rate limiting in Redis / Valkey.
- Add PostgreSQL for durable job history and saved summaries.
- Add streaming progress updates over WebSockets or SSE.
