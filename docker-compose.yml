services:
  valkey:
    image: valkey/valkey:8
    container_name: insightflow-valkey
    ports:
      - "6379:6379"
    volumes:
      - valkey_data:/data
    command: valkey-server --save 60 1 --loglevel warning
    restart: unless-stopped

volumes:
  valkey_data:
